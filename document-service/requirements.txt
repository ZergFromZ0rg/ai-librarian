fastapi==0.140.0
uvicorn[standard]==0.52.4
numpy==2.4.0
PyMuPDF==1.28.2
pymupdf4llm==1.28.2
python-multipart==0.0.32
requests==2.32.5
httpx==0.28.1
torch==2.13.0
sentence-transformers==6.0.0
qdrant-client==1.19.0

# Ask mode: the native client for the Claude provider. Imported lazily by
# generation.py, so a deployment that only uses Ollama / OpenAI / Gemini never
# loads it (or its httpx2 dependency). Other providers use the OpenAI-compatible
# REST path over httpx and need no SDK.
anthropic==1.3.0
